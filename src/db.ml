open Ser
open Hash

let dbopen dir =
  raise (Failure "to be written")

let dbclose () =
  raise (Failure "to be written")

let dbget d k seival =
  raise (Failure "to be written")

let dbexists d k =
  raise (Failure "to be written")

let dbput d k v seoval =
  raise (Failure "to be written")

let dbdelete d k =
  raise (Failure "to be written")
